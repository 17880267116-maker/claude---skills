---
name: social-crawler
description: 小红书博主数据爬取。按博主名 + 时间/条数/互动阈值筛选视频/图文，返回点赞/评论/收藏/转发数据。触发：/crawl、爬取博主、抓数据、social-crawler
---

# 社交媒体数据爬取 Skill

## 当前能力

| 平台 | 状态 | 说明 |
|------|------|------|
| 小红书 | ✅ | 基于 TikHub API |
| 抖音 | ❌ | TikHub 不支持，待找 API |
| 视频号 | ❌ | 无可用 API |

## 触发方式

```
/crawl 博主名
爬取 XXX 最近一个月的数据
帮我抓 XXX 的笔记，只要点赞超过100的
```

## 可用筛选条件

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 博主名 | 小红书博主昵称或 user_id | 必填 |
| `--days` | 最近 N 天 | 30 |
| `--max` | 最多取 N 条 | 0（不限） |
| `--min-likes` | 最低点赞数 | 0（不限） |
| `--type` | video / normal / all | video |
| `--output` | 输出目录 | ./data |

## 返回数据

CSV 文件，包含：标题、文案、点赞、评论、收藏、转发、发布时间、笔记链接

## 执行流程

1. 通过 TikHub API 搜索定位博主 user_id
2. 拉取博主主页笔记列表（支持分页）
3. 逐条获取详情（含完整互动数据 + 发布时间）
4. 按条件筛选（时间 / 条数 / 互动阈值）
5. 输出 CSV + JSON

## 使用示例

```
# Claude Code 中直接说：
爬取 好运聊AI 最近30天的视频
帮我抓 XXX 最近一个月的笔记，只要点赞过100的
/crawl 好运聊AI --days 14 --max 10 --min-likes 50 --type all
```
